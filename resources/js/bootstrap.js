// Frontend bootstrap intentionally keeps runtime dependencies minimal.
// The application currently does not use Axios directly; page scripts use
// fetch()/FormData and Laravel's CSRF token instead.
